version: 2

project_name: tokencontrol

builds:
  - main: ./cmd/tokencontrol/
    binary: tokencontrol
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X github.com/ppiankov/tokencontrol/internal/cli.Version={{.Version}}
      - -X github.com/ppiankov/tokencontrol/internal/cli.Commit={{.ShortCommit}}
      - -X github.com/ppiankov/tokencontrol/internal/cli.BuildDate={{.CommitDate}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

dockers:
  - image_templates:
      - "ghcr.io/ppiankov/tokencontrol:{{ .Version }}-amd64"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    build_flag_templates:
      - "--platform=linux/amd64"
    goarch: amd64

  - image_templates:
      - "ghcr.io/ppiankov/tokencontrol:{{ .Version }}-arm64"
    dockerfile: Dockerfile.goreleaser
    use: buildx
    build_flag_templates:
      - "--platform=linux/arm64"
    goarch: arm64

brews:
  - repository:
      owner: ppiankov
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    directory: Formula
    homepage: "https://github.com/ppiankov/tokencontrol"
    description: "Dependency-aware parallel task runner for AI coding agents"
    license: BUSL-1.1
    install: |
      bin.install "tokencontrol"
    test: |
      system "#{bin}/tokencontrol", "version"

docker_manifests:
  - name_template: "ghcr.io/ppiankov/tokencontrol:{{ .Version }}"
    image_templates:
      - "ghcr.io/ppiankov/tokencontrol:{{ .Version }}-amd64"
      - "ghcr.io/ppiankov/tokencontrol:{{ .Version }}-arm64"

  - name_template: "ghcr.io/ppiankov/tokencontrol:latest"
    image_templates:
      - "ghcr.io/ppiankov/tokencontrol:{{ .Version }}-amd64"
      - "ghcr.io/ppiankov/tokencontrol:{{ .Version }}-arm64"
